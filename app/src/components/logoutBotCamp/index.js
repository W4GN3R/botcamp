import styled from "styled-components";

import iconLogout from "../../img/icon-logout.svg";

const Logout = styled.img.attrs({
    src: iconLogout,
})`
    width: 30px;
    height: 30px;
`;

export default Logout;